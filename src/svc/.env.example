# Optional local override for `make -C src/svc run`.
# Compose and production knobs live in the repo-root `.env.example`.

DCALCON_PUBLIC_URL=http://localhost:8080
DCALCON_ADMIN_USERNAME=admin
DCALCON_ADMIN_PASSWORD=dcalcon-dev-pass
DCALCON_SESSION_SECURE=false
